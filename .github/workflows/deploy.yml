name: Deploy to GitHub Pages

# DISABLED: This app uses API routes and server-side features that require a Node.js runtime.
# GitHub Pages only supports static sites. Use Azure Web App deployment instead.
on:
  workflow_dispatch:  # Manual trigger only (effectively disabled)
  # push:
  #   branches: [ master ]

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build (static export)
        run: npm run build

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './out'

      - name: Deploy to Pages
        id: deployment
        uses: actions/deploy-pages@v4
